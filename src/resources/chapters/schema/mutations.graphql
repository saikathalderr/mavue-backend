#import "./index.graphql"

input ChapterInput {
  title: String!
  text: String
  requirements: String!
  recurringInterval: Int!
  userId: String
}

type Mutation {
  createChapter(input: ChapterInput!): Chapter
}